{
   "apiVersion": "kustomize.toolkit.fluxcd.io/v1beta2",
   "kind": "Kustomization",
   "metadata": {
      "name": "vault-config-operator",
      "namespace": "flux-system"
   },
   "spec": {
      "dependsOn": [
         {
            "name": "vault-config-operator-configuration"
         }
      ],
      "interval": "10m",
      "path": "./fluxcd/manifests/test/infrastructure/vault-config-operator/main",
      "prune": true,
      "sourceRef": {
         "kind": "GitRepository",
         "name": "k3s-cluster-deploy"
      },
      "wait": true
   }
}
