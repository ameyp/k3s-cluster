apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: infra-configs-certs
  namespace: flux-system
spec:
  dependsOn:
    - name: infra-configs-clusterissuers
  interval: 10m
  sourceRef:
    kind: GitRepository
    name: k3s-cluster-deploy
  path: "./tanka/manifests/infrastructure/configs/certs"
  prune: true
